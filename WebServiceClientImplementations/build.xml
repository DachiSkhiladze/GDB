<?xml version="1.0"?>
<project name="WebServicesClientImplementations" basedir="." default="jar">
	<property file="build.properties" />
	
	<path id="build.classpath">
	      <pathelement location="lib/reload4j-1.2.18.3.jar"/>
	      <pathelement location="lib-gen/jaxb-2.2/jaxb-api.jar"/>
	</path>
	
	<target name="clean">
		<delete dir="${result.dir}" />
		<delete dir="${stage.dir}" />
	</target>
	
	<target name="compile" depends="compile-generated">
		<mkdir dir="${classes.dir}"/>
		<javac srcdir="${src.dir}"
         destdir="${classes.dir}"
         classpathref="build.classpath"
         debug="on" includeantruntime="false" />
	</target>
	
	<target name="compile-generated">
		<echo message="From WebserviceClientImplementations"></echo>
		<pathconvert property="build_path" refid="build.classpath"></pathconvert>
		<echo message="build.classpath: ${build_path}"></echo>
		<mkdir dir="${classes.dir}"/>
		<javac srcdir="${src.gen.dir}"
         destdir="${classes.dir}"
         classpathref="build.classpath"
         debug="on" includeantruntime="false" />
	</target>
	
	<target name="jar" depends="compile">
		<mkdir dir="${result.dir}"/>
		<copy todir="${classes.dir}" includeEmptyDirs="false">
				<fileset dir="${src.dir}" includes="**/*.html,**/*.xsd"/>
		</copy>
		<jar destfile="${result.dir}/${jar.name}" basedir="${classes.dir}"></jar>
	</target>
	
	<target name="deploy" depends="jar">
		<copy file="${result.dir}/${jar.name}" todir="${deploy.project.dir}"></copy>
	</target>
	
</project>

