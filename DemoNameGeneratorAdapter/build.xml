<?xml version="1.0"?>
<project name="DemoNameGeneratorAdapter" basedir="." default="jar">
	<property file="build.properties" />

	<path id="build.classpath">
		<pathelement location="lib/biologics-adapter.jar"/>
		<pathelement location="lib/reload4j-1.2.18.3.jar"/>
	</path>

	<target name="clean">
		<delete dir="${classes.dir}" />
		<delete dir="${result.dir}" />
	</target>

	<target name="prepare">
		<mkdir dir="${classes.dir}" />
		<mkdir dir="${result.dir}" />
	</target>

	<target name="compile" depends="prepare">
		<javac srcdir="${src.dir}"
			destdir="${classes.dir}"
			classpathref="build.classpath"
			debug="on" includeantruntime="false" />
	</target>

	<target name="jar" depends="compile">
		<jar destfile="${result.dir}/${jar.name}" basedir="${classes.dir}"/>
	</target>

	<target name="deploy" depends="jar">
		<copy file="${result.dir}/${jar.name}" todir="${deploy.project.dir}"/>
	</target>
</project>
